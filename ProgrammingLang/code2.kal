
def isPrime(n)
    i = 2
    while i < n
        if (n % i) = 0
            return 0
        i = i + 1
    return 1

def primes(n)
    i = 0
    while i < n
        if isPrime(i)
            say (i) " is primey!"
        i = i + 1
¨
say (primes(100000))