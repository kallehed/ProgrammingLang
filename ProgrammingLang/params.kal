

say "params file"

def func(a,  b,   c, d, e)
    say "func:" (a) " and " (b) " ALSO " (c) " EVEN: " (d) " OASKD " (e)
    return a * b + c
    
x = func(3,4,5, 6, 7)  * 2

say "x is " (x)

def saynum(num, times)
    i = 0
    while i < times
    
        say "NUM: " (num * i)
        i = i + 1
    return num*times

say (saynum(420, 3) * 2)
say "correct: " (420*6)